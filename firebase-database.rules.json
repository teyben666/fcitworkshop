{
  "rules": {
    "rooms": {
      "$roomId": {
        ".read": true,
        ".write": "auth != null || true",
        ".validate": "newData.hasChildren(['id', 'hostId', 'status'])"
      }
    }
  }
}
