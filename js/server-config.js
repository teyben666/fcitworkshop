/**
 * Currency Safe — server sync config.
 * 必须用 npm start 的地址打开（默认 :8787），不要用 python :8080
 */
window.CURRENCY_SAFE_SERVER = {
    enabled: true,
    /** "auto" = 当前网址 /ws，并自动尝试 :8787 */
    wsUrl: "auto"
};
